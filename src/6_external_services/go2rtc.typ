== Go2rtc

Сервис конвертирования видеопотока с видеокамеры.

=== docker

#raw(
  lang: "yaml",
  read("../config_services/go2rtc/docker-compose.yml")
)

=== `./config_services/go2rtc/go2rtc.yaml`

#raw(
  lang: "yaml",
  read("../config_services/go2rtc/go2rtc.yaml")
)
